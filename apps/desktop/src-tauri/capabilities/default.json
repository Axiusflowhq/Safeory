{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the main Safeory window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-save"]
}
